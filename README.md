# ELC Secure Network Configuration

Symmetric encryption module (Caesar, Playfair, Hill ciphers).

## Quick Start

```bash
pip install -r requirements.txt
python run.py --encrypt    # Encrypt + generate graphs
python run.py --pdf       # Generate submission PDF
```

See **[docs/README.md](docs/README.md)** for full documentation.
